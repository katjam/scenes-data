{
  "name": "scenes-data",
  "version": "0.0.0",
  "description": "Extracts data from screenplays for schedule/ budget",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "rm -r dist && parcel build src/index.html --public-url ./",
    "test": "elm-test",
    "autotest": "elm-test --watch"
  },
  "author": "katjam",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "elm": "^0.19.0",
    "elm-hot": "^1.1.4",
    "elm-test": "^0.19.1",
    "node-elm-compiler": "^5.0.4",
    "parcel-bundler": "^1.12.4"
  }
}
